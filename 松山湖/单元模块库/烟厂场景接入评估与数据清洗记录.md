---
title: "烟厂场景接入评估与数据清洗记录"
summary: "评估卷烟厂场景作为第三个真实结构场景的接入可行性，记录 Excel 数据清洗、原始数据核查和后续通用后端接入边界。"
tags:
  - 烟厂场景
  - 第三场景
  - 数据清洗
  - future_generic
  - 工业蒸汽
updated_at: "2026-05-27"
related:
  - "课题组场景整理模板_烟厂.xlsx"
  - "课题组场景整理模板_烟厂_清洗版.xlsx"
  - "项目责任边界与对接架构讨论记录.md"
  - "场景化系统设计接口与典型日输入规范.md"
---

# 烟厂场景接入评估与数据清洗记录

## 1. 结论

烟厂场景可以作为第三个真实结构场景接入，而且比继续使用松山湖变体更适合证明单元模块库的通用性。

它不应走旧版 `current_cchp` 后端，而应走 `future_generic` / 通用模块后端。原因是旧版 CCHP 后端只支持固定冷热电结构，而烟厂场景涉及工业蒸汽、余热回收、热泵、储热水箱、太阳能资源和工业分时电价等内容。

当前适合的验收定位是：

1. 作为“第三个真实结构场景 / 单元模块库接口验收场景”：可以接入。
2. 作为“真实可计算容量优化场景”：还需要补齐通用后端设备映射、蒸汽/热水单位边界、数据文件导出和部分经济参数。

## 2. 已检查文件

| 文件 | 用途 | 核查结论 |
|---|---|---|
| `课题组场景整理模板_烟厂.xlsx` | 师弟整理的烟厂场景 Excel | 结构完整，但有负荷重复、错位和资源曲线不完整问题 |
| `课题组场景整理模板_烟厂_清洗版.xlsx` | 本次生成的清洗副本 | 04 表变为 864 行，05 表扩展为 1440 行，不覆盖原文件 |
| `烟厂场景/轻量化模型-Tobacoo_modeling20260508/Tobacoo_modeling/01_data_clean/load_data.csv` | 原始运行数据 | 实际是 Excel 工作簿格式，包含生产、天气、冷热负荷、太阳能等 sheet |
| `烟厂场景/重点研发结题材料0524(2).docx` | 项目结题材料 | 找到热泵容量、储热水箱、总投资和蒸汽替代信息 |

## 3. 场景特征

烟厂场景当前 Excel 中的主要内容如下：

| 类别 | 内容 |
|---|---|
| 场景编号 | `tobacco_factory_001` |
| 场景类型 | `tobacco_factory_multi_energy` |
| 用户需求 | 电负荷、冷负荷、蒸汽负荷 |
| 能源输入 | 电网购电、天然气 |
| 资源输入 | 太阳辐射、余热、环境温度 |
| 设备 | 光伏、燃气 CHP、电制冷、吸收式制冷、电储能、冷储能、蒸汽锅炉、余热回收 |
| 清洗后新增/启用建议 | 电热泵、热储能 |

这个结构与松山湖/德国的“冷热电 CCHP”不同，能够体现“工业电-冷-蒸汽/热水-余热-天然气”的多能转换场景。

## 4. 原始数据核查

`load_data.csv` 文件虽然扩展名是 `.csv`，但文件头是 `PK`，实际是 Excel 工作簿。它包含这些 sheet：

| sheet | 数据含义 | 时间范围/分辨率 |
|---|---|---|
| `production_data` | 生产计划 | 2025-01-01 至 2026-01-27，日尺度，部分缺日 |
| `weather_data` | 太阳辐射、室外温度、湿度 | 2025-01-01 至 2026-01-27，30 min |
| `ZS_JB_data` | 制丝/卷包供热相关负荷与水系统数据 | 2025-09-01 至 2026-01-27，30 min |
| `solar_data` | 太阳能系统温度和泵状态 | 2025-10-01 至 2025-12-24，30 min，部分时间缺失 |
| `cooling_load` | 制丝/卷包冷负荷及分项冷量 | 2025-01-01 至 2026-01-27，30 min |

因此，该场景数据不是纯手工模板，背后有真实运行/项目数据支撑。但 Excel 中的 12 个月典型日负荷仍带有整理和合成性质，不能直接等同于完整实测 8760 数据。

## 5. Excel 数据质量问题与处理

### 5.1 `04_用户负荷需求曲线`

原始问题：

| 问题 | 原始表现 | 处理 |
|---|---|---|
| 行数异常 | 应为 12 个月 x 24 小时 x 3 类负荷 = 864 行，原始为 894 行 | 删除重复组合 |
| 1 月前 10 小时重复 | 1 月 hour 1-10 的电/冷/蒸汽各重复一次，共 30 行重复 | 保留第一组，删除重复组 |
| 4 月蒸汽第 13 小时错位 | `M04/4/13/steam` 被写入 `profile_id`，导致字段错位 | 修正为 `M04, 4, 13, steam, 6.2, t/h` |
| 数值末尾逗号 | 个别单元格出现 `6.5,`、`1550,` 等手工格式 | 统一去掉逗号并转为数值 |

清洗后结果：

| 指标 | 结果 |
|---|---|
| 行数 | 864 |
| 重复组合 | 0 |
| 缺失组合 | 0 |
| 覆盖范围 | 12 个月 x 24 小时 x electricity/cooling/steam |

### 5.2 `05_能源输入与资源曲线`

原始问题：

| 问题 | 原始表现 |
|---|---|
| 月份不完整 | 只有 1、4、7、10 月 |
| 小时不完整 | 每月只有 1 点和 12 点 |
| 资源曲线不足 | 不足以直接支撑 12 个月典型日调度 |

处理方式：

1. 电网购电和天然气：扩展为 12 个月 x 24 小时，购能上限仍留空，表示不限。
2. 太阳辐射和环境温度：从原始 `weather_data` 中按 2025 年数据生成月-小时平均值。
3. 余热：由于完整余热资源曲线尚不明确，先基于原 Excel 的季节性锚点进行月度和小时插值/合成，并在备注中标记为 synthesized。

清洗后结果：

| input_id | 行数 |
|---|---:|
| `grid_electricity` | 288 |
| `natural_gas` | 288 |
| `solar_resource` | 288 |
| `waste_heat` | 288 |
| `temperature` | 288 |
| 合计 | 1440 |

## 6. 结题材料中找到的信息

### 6.1 设备容量上界

结题材料第 4 章建设内容中明确提到三类改造区域：

| 区域 | 设备 | 最大制热量 |
|---|---|---:|
| 动力中心地下管廊 | 2 台磁悬浮热泵机组 | 900.1 kW + 1314 kW |
| 联合工房制丝侧 | 1 台水源热泵、2 台四管制风冷热泵、1 台风冷热泵 | 1326.8 kW + 2 x 1513.5 kW + 1386 kW |
| 联合工房卷包侧 | 2 台水源热泵、2 台四管制风冷热泵、1 台风冷热泵 | 1326.8 kW + 243.7 kW + 2 x 1513.5 kW + 1386 kW |

合计最大制热量约为：

```text
900.1 + 1314 + 1326.8 + 2 * 1513.5 + 1386 + 1326.8 + 243.7 + 2 * 1513.5 + 1386
= 13937.4 kW
```

因此，清洗版 Excel 中将：

- `electric_heat_pump.capacity_ub = 13937.4 kW`
- `waste_heat_recovery.capacity_ub = 13937.4 kW`，作为接口验证阶段的保守约束，后续应替换为可回收余热实际上限。

### 6.2 储热水箱

结题材料给出：

- 制丝侧新增 `567 m3` 储热水箱；
- 卷包侧新增 `900 m3` 储热水箱；
- 合计 `1467 m3`。

清洗版 Excel 中临时将其折算为热储能能量容量：

```text
1467 m3 * 1000 kg/m3 * 4.186 kJ/(kg*K) * 15 K / 3600
= 25586.9 kWh
```

注意：这里假设有效温差为 15 K，仅用于接口验证。真实建模时需要导师或项目资料确认储热温区、可用温差、热损失和运行上下限。

### 6.3 蒸汽与热水边界

结题材料中明确提到空调系统适配性改造：将原蒸汽盘管替换为适配 45 摄氏度热水参数的热水盘管，用热水替代蒸汽后每年可减少蒸汽消耗 24000 吨。

这说明烟厂场景里“蒸汽需求”可能有两种表达方式：

1. 原系统口径：以 `steam` 表示原蒸汽消耗需求，单位为 `t/h` 或 `t`。
2. 改造后口径：以 `heat` 或 `hot_water` 表示 45 摄氏度以上热水供热需求，单位为 `kW/kWh`。

因此，后续接入模型时需要明确：优化器到底直接处理 `steam`，还是先把蒸汽替代量折算成热水热负荷。当前建议保留 `steam` 作为用户需求载体，同时在通用后端预留 `steam_to_heat` 或 `high_grade_heat_source` 转换。

### 6.4 投资成本

结题材料中找到项目批复总投资额控制 `4077 万元`，但没有找到单设备投资成本拆分。因此：

- 可以用于场景总投资背景说明；
- 不适合直接作为每类设备的投资成本；
- 真实优化前仍建议使用默认参数库补齐单设备投资成本，并在报告中标记来源。

## 7. 清洗版 Excel 改动摘要

本次没有覆盖原始文件，而是新增：

`课题组场景整理模板_烟厂_清洗版.xlsx`

主要改动：

| sheet | 改动 |
|---|---|
| `04_用户负荷需求曲线` | 删除 30 行重复数据，修复 4 月 13 时蒸汽错位行，统一数值格式 |
| `05_能源输入与资源曲线` | 扩展为 12 个月 x 24 小时 x 5 类输入/资源，共 1440 行 |
| `06_候选设备配置` | 启用电热泵和热储能，补充热泵 13937.4 kW、储热 25586.9 kWh 等物理上界 |
| `08_资料来源与缺口` | 将设备容量上限和投资成本状态更新为“部分获得” |
| `09_备注` | 添加本次数据清洗记录 |

## 8. 当前仍不能直接求解的原因

清洗版 Excel 已经适合作为第三真实结构场景输入，但还不能直接用旧求解后端跑容量优化，主要原因如下：

1. Excel 导入器仍默认写入 `system.template = cchp_ehc_base`，需要为 `tobacco_factory_multi_energy` 映射新的通用模板。
2. `current_cchp` 不支持 `steam_boiler`、`waste_heat_recovery`、`electric_heat_pump`、`heat_storage` 等烟厂设备。
3. Excel 导入器目前没有导出 `energy_capacity_ub`，导致热储能能量容量上界还不能进入 `scenario.yaml`。
4. 24 小时分时电价目前会被简化为 flat price，需要保留为 24h TOU 数组。
5. `load_file` 和 `typical_day_file` 仍未自动生成到场景配置中，需要把 04/05 表导出为通用后端可读的数据文件。
6. 蒸汽 `t/h` 与热功率 `kW` 的换算边界尚未定稿；如果直接以 `steam` 建模，需要单独的蒸汽载体和设备约束。

## 9. 建议接入路径

推荐路径：

```text
课题组场景整理模板_烟厂_清洗版.xlsx
  -> ExcelScenarioParser
  -> tobacco_factory_multi_energy scenario.yaml
  -> system.template = tobacco_factory_multi_energy 或 industrial_steam_cooling_base
  -> DefaultsResolver 补齐技术经济参数
  -> GenericBackendPlanner 生成组件计划
  -> GenericModelBuilder 生成通用系统模型
  -> evaluate_dispatch(capacities) / build_dispatch_model()
  -> demo 优化闭环验证
```

不推荐路径：

```text
烟厂 Excel -> cchp_ehc_base -> current_cchp
```

## 10. 已完成的接入实现

本次已完成第一阶段代码接入，范围是“第三真实结构场景 + 通用组件计划 + 通用模型构建材料”，暂不把烟厂套入旧版 `current_cchp`。

已新增/修改：

| 内容 | 文件 |
|---|---|
| 烟厂系统模板 | `ies_design/defaults/system_templates.yaml` 中新增 `tobacco_factory_multi_energy` |
| 场景目录映射 | `ies_design/defaults/scenario_catalog.yaml` 中新增 `tobacco_factory_multi_energy` |
| Excel 模板选择 | `ies_design/excel_parser.py` 按 `scenario_type` 选择烟厂通用模板 |
| Excel 字段保留 | `ies_design/excel_parser.py` 保留 `energy_capacity_ub_kwh` 和 24h 分时电价 |
| 相对数据文件校验 | `ies_design/schema_validator.py` 支持相对 `scenario.yaml` 所在目录解析负荷/资源文件 |
| 储能能量容量变量 | `ies_design/generic_backend_planner.py` 支持 `energy_capacity_ub_kwh` |
| 多能转换类型统计 | `generic_component_plan`、`generic_model_components`、`generic_design_solutions` 均输出转换/模块类型数量和清单 |
| 储能容量缺口提示 | `schema_validator.py` 与 `generic_backend_planner.py` 区分“已有能量容量上界”和“缺少功率容量上界” |
| 烟厂场景包 | `ies_design/scenarios/tobacco_factory/` |

烟厂场景包包含：

| 文件 | 说明 |
|---|---|
| `scenario.yaml` | 标准场景配置，模板为 `tobacco_factory_multi_energy` |
| `typical_profiles.csv` | 12 个月典型日电/冷/蒸汽负荷 |
| `input_resource_profiles.csv` | 电网、天然气、太阳辐射、余热、温度输入/资源曲线 |
| `data_gaps.csv` | 数据来源与缺口 |
| `excel_parse_warnings.txt` | Excel 导入警告 |
| `README.md` | 场景说明 |

已验证命令：

```powershell
rtk uv run python "松山湖\单元模块库\ies_design\tests\test_excel_parser.py"
rtk uv run python "松山湖\单元模块库\ies_design\tests\validate_default_configs.py"
rtk uv run python "松山湖\单元模块库\ies_design\tests\test_design_cli.py"
rtk uv run python design.py --scenario "松山湖\单元模块库\ies_design\scenarios\tobacco_factory\scenario.yaml" --validate-only --accept-future
rtk uv run python design.py --scenario "松山湖\单元模块库\ies_design\scenarios\tobacco_factory\scenario.yaml" --export-component-plan --build-generic-model --accept-future --output ".codex_tmp_tobacco_build_check"
rtk uv run python design.py --scenario "松山湖\单元模块库\ies_design\scenarios\tobacco_factory\scenario.yaml" --run-generic-design --generic-search-levels 0 1 --accept-future --output ".codex_tmp_tobacco_generic_design_check"
```

验证结果：

| 检查 | 结果 |
|---|---|
| Excel 解析测试 | 通过 |
| 默认配置测试 | 通过 |
| CLI 回归测试 | 通过 |
| 烟厂场景校验 | `future_supported` |
| 组件计划导出 | 通过 |
| 通用模型构建材料导出 | 通过 |
| 通用设计搜索材料导出 | 通过，输出 `conversion_type_count=8` |
| 热储能容量提示 | 已改为“已有能量容量上界，但缺少功率容量上界” |

通用组件计划已经能识别以下烟厂关键模块：

| 设备 | 抽象类型 | 通用组件 |
|---|---|---|
| `steam_boiler` | `fuel_to_steam` | `Transformer` |
| `waste_heat_recovery` | `recoverable_energy_to_heat` | `Source_plus_Transformer` |
| `electric_heat_pump` | `power_to_heat` | `Transformer` |
| `heat_storage` | `storage` | `GenericStorage` |

其中 `heat_storage.capacity_kwh` 已能读取清洗版 Excel 中的 `25586.9 kWh`。

当前烟厂场景的多能转换/模块类型统计为 8 类：

| 抽象类型 | 代表设备 | 输入载体 | 输出载体 |
|---|---|---|---|
| `renewable_power` | `pv` | 太阳辐射资源 | 电 |
| `cogeneration` | `chp` | 天然气 | 电、热 |
| `power_to_heat` | `electric_heat_pump` | 电 | 热 |
| `power_to_cooling` | `electric_chiller` | 电 | 冷 |
| `heat_to_cooling` | `absorption_chiller` | 热 | 冷 |
| `storage` | `electric_storage`、`heat_storage`、`cold_storage` | 同载体输入 | 同载体输出 |
| `fuel_to_steam` | `steam_boiler` | 天然气 | 蒸汽 |
| `recoverable_energy_to_heat` | `waste_heat_recovery` | 余热 | 热、蒸汽 |

## 11. Level 3 真实求解验收记录（2026-05-28）

烟厂场景已经从“第三真实结构场景 / 组件计划验收”推进到“Level 3 真实场景 + 通用线性 Energy Hub 调度求解 + 容量结果导出”。该场景仍标记为 `future_supported`，表示它不走旧版 `current_cchp` 后端；但通过显式参数 `--solve-generic-dispatch --accept-default-bounds` 可以进入通用后端真实求解路径。

验收命令：

```bash
rtk uv run python design.py --scenario "松山湖\单元模块库\ies_design\scenarios\tobacco_factory\scenario.yaml" --run-generic-design --generic-search-levels 1.0 --solve-generic-dispatch --dispatch-month 1 --dispatch-periods 24 --accept-future --accept-default-bounds --output "DesignResults/tobacco_factory_level3_acceptance"
```

统一检查命令：

```bash
rtk uv run python run_design_checks.py --include-tobacco-level3
```

验收结果包：

| 文件 | 用途 |
|---|---|
| `DesignResults/tobacco_factory_level3_acceptance/generic_design_solutions.json` | 完整候选方案、容量向量、通用模型、OEMOF 调度求解状态和能流摘要 |
| `DesignResults/tobacco_factory_level3_acceptance/generic_design_solutions.csv` | 候选方案目标值和容量变量扁平表 |
| `DesignResults/tobacco_factory_level3_acceptance/generic_design_report.md` | Level 3 验收报告，标记真实求解来源为通用线性 Energy Hub |
| `DesignResults/tobacco_factory_level3_acceptance/capacity_solution.csv` | 最优/验收方案设备容量结果，含 `steam_boiler.steam_capacity_t_h` 等动态容量变量 |
| `DesignResults/tobacco_factory_level3_acceptance/dispatch_summary.csv` | 调度求解摘要，含 `scope=linear_energy_hub`、求解器和目标函数值 |
| `DesignResults/tobacco_factory_level3_acceptance/energy_flow_summary.csv` | 电、冷、热、蒸汽、天然气、余热等能流汇总 |
| `DesignResults/tobacco_factory_level3_acceptance/conversion_type_summary.csv` | 多能转换类型数量和设备映射统计 |

当前真实求解边界：

- 负荷侧：读取烟厂 12 个月典型日数据，当前验收命令求解 1 月 24h。
- 蒸汽侧：内部将 `t/h` 按统一载体单位注册表换算为 `kW_th`，报告保留原变量名与单位提示。
- 资源侧：05 表资源曲线允许加载或按月典型日合成；当前已支持太阳辐射、余热、环境温度。
- 容量侧：用户未填的关键上界可通过 `--accept-default-bounds` 使用验收默认值，并在容量空间中记录 `bound_source=acceptance_default`。
- 后端侧：使用通用 Source / Sink / Transformer / GenericStorage 组件映射生成 OEMOF/Solph 线性模型，不依赖旧版固定 9 变量 CCHP。

## 12. 后续开发任务建议

1. 明确蒸汽换算口径：
   - 如果保留 `steam`：模型内以 `t/h` 建模；
   - 如果折算为热负荷：需要蒸汽压力、温度、焓值或统一折算系数。
2. 补齐设备经济参数：特别是 `steam_boiler`、`waste_heat_recovery`、热泵、储能的投资成本。
3. 补齐剩余容量上界：光伏、CHP、电制冷、吸收式制冷、电储能、冷储能、蒸汽锅炉。
4. 将 `future_generic` 从组件计划推进到真实 Pyomo/OEMOF 下层调度求解。
5. 将多能转换类型统计进一步接入最终验收版 Excel/Word 汇总报告。

## 13. 需要继续确认的问题

1. 烟厂场景最终是按“原蒸汽系统需求”建模，还是按“改造后 45 摄氏度热水供热需求”建模？
2. 蒸汽 `t/h` 是否保留为独立能源载体，还是统一折算为 `kW` 热负荷？
3. 热储能的有效温差是否可以按 15 K，还是需要按设计供回水温度重新计算？
4. 光伏装机上限是否有屋顶面积或项目资料支撑？当前结题材料只提到少量光伏，没有找到屋顶面积。
5. CHP 是否确实属于该烟厂场景候选设备？结题材料主线更偏余热回收、热泵、太阳能光热和储热。
6. 蒸汽锅炉容量上界是否有原系统锅炉额定容量资料？
7. 单设备投资成本能否从工程清单获得？如果不能，是否同意先使用默认参数库。
