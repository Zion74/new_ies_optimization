# E0-D-30 物理可达域与年度服务紧化产物

本目录保存 D30 的双窗口界限筛查和全局证书：

- `screen_24h.json` / `screen_336h.json`：逐时静态物理外包络、年度服务传播与符号宽度审计；
- `24h.json`：24 h 等价性门与精确点保留审计；
- `336h.json`：336 h、1800 s、28 线程的 reopened global L1 探针；
- `e0d30_physics_service_bound_certificate.csv`：排除运行时间的确定性双窗口汇总；
- `manifest.json`：正式数据、D19/D22/D29、源码、筛查和探针的 SHA-256 锁；
- `execution.json`：包含运行时间的非规范 sidecar。

D30 在 D29 之上每时段新增 6 条区间感知有效不等式，不固定主整数或符号二元，不改变整数可行集。24 h 保留 D29 精确点 `26,010.174929 MWh/a`；336 h 首选严格区间为 `[36,382.462799,777,141.368858] MWh/a`，上界较 D29 改善 `8.0363%`，但仍未闭合。

本目录不包含实际价格、正式 TAC 或技术赢家。
