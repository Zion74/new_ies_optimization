# `archive/` 说明

- **`superseded_pre_IEMI/`**：在 **IEMI + 会议 LaTeX 定稿** 之前的 Markdown/讨论稿，叙事包括：
  - 能质系数嵌入**波动率（标准差）**的旧插件思路；
  - 「Entropy-weighted」「EQD」等与**当前会议主线**不一致或已被期刊稿独占的表述；
  - 重复保存的 `Final` / `gemini` 版本等。

**请勿**将这些文件当作当前投稿正文来源。正式写作与投稿以根目录 **`main_conference.tex`** 为准。

若从历史稿中恢复某段文字，须与 `sections_conference/` 的 IEMI 定位及 `data/source_manifest.txt` 中的实验设定逐句核对。
