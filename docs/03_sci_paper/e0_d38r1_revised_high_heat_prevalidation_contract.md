# E0-D-38-R1 高热紧 PCC 状态一次性修订合同

状态：**结果前预注册已冻结并执行；baseline 无储能发生代表期可行、真实 8784 h 不可行的时间聚合反转，R1 三状态合同失败**

日期：2026-07-15

## 1. 修订原因与不可覆盖边界

原 D38 `H*=0.80/G*=0.70` 状态在真实 8784 h 无储能最小弃电阶段物理不可行。490 MW PCC 下双 CHP 静态最大供热为 `766.0767880248933 MWth`，原高热序列有 36 h 超限且全部已在 D36 代表周 4。原失败及合同永久保留，不能被 R1 结果覆盖或改写为“原 D38 通过”。

R1 只修订高热状态的热需求尺度；baseline、24 h 长时状态、PCC、代表周、成本、服务生成算法、10% 弃电帽、求解 gap 和验收阈值全部沿用原 D38 合同。

## 2. 一次性修订状态

修订状态固定为 `high_heat_tight_pcc_r1`：

- PCC 保持 `490 MW`，即 `G*=0.70`；
- 热需求目标固定为 `H*=0.70`，与紧 PCC 形成对称的 `H*=G*=0.70` 压力状态；
- 两机用户侧最大供热仍为 `876 MWth`；
- 实际 2024 热需求 `P95=384.15476851851884 MWth`；
- 目标 P95 为 `0.70×876=613.2 MWth`；
- 全时段热量固定乘 `1.5962316499799991`；
- 修订后峰值为 `724.03367951984 MWth`；
- 490 MW PCC 下静态无储能最大供热为 `766.0767880248933 MWth`，峰值保留 `42.0431085050533 MWth` 静态余量。

选择 `0.70/0.70` 是一次性、可解释的对称状态，不根据 BESS/TES 成本、容量、排序或代表期误差调参。R1 不允许再搜索 `H*` 或 `G*`；若真实全年无储能参考仍不可行，D38 终止并登记失败。

## 3. 服务与求解合同

R1 的年度 PCC 目标仍只由修订状态的真实 8784 h 无储能两阶段求解产生：先最小弃电，再在最小弃电 `+1e-3 MWh/a` 内最小燃料。主弃电帽仍是实际全年可再生可用量的 10%，同一绝对 MWh 帽和 PCC 目标原样用于代表期和全年。

第一阶段燃料价格严格为零。执行器允许把对弃电目标冗余的对数燃料段编码位放松为连续变量；该放松只改变未计价燃料插值，不改变 CHP 功率、供热、启停、爬坡、PCC 或弃电可行域投影。第二阶段及所有经济/容量任务保留完整离散燃料段。该等价实现已用小模型对照测试并纳入完整回归。

求解序列、完整容量快照字段、HiGHS `0.1%` gap、有限 incumbent/dual 判定、成本/弃电/燃煤/后悔值/容量/5% 无差异集合验收全部沿用原合同，不在 R1 重定义。

## 4. 复用与文件隔离

- baseline 与 `long_duration_24h` 的物理服务仍相同，可复用同一 baseline 服务 JSON；
- 只有输入哈希、服务参数、模型代码哈希和求解容差一致时，才可复用已完成的 baseline/long-duration 结果；
- 原失败文件 `service_high_heat_tight_pcc.json` 永不覆盖；
- R1 服务文件固定为 `service_high_heat_tight_pcc_r1.json`，后续任务文件名必须包含 `high_heat_tight_pcc_r1`；
- R1 产物与原失败记录同时进入 manifest。

## 5. 通过、失败与主张边界

R1 只有在 baseline、`high_heat_tight_pcc_r1`、`long_duration_24h` 三状态全部满足原 D38 验收门时，才能关闭“修订后三状态时间聚合预验证”。论文必须同时披露原 `0.80/0.70` 状态因服务参考物理不可行而失败，以及 R1 如何在任何储能结果产生前冻结。

R1 仍只允许报告时间聚合门和公开成本代理排序，不产生杨凌正式 TAC、项目技术赢家或 E2/E3/E4 批量边界结论。D24/D25 和项目级 VOM/碳/结算账户继续独立阻断正式经济结论。

## 6. 执行后记录

当前代码与同一输入 provenance 下，baseline 服务合同 SHA-256 为 `93f3d7b5c50312d08ea3dd78b1af70661facf880fcc882b1bb1ac32a783977b3`。代表期无储能在 10% 弃电帽下完成，弃电 `338,777.027 MWh`、PCC 残差 `4.19e-9 MWh`；真实 8784 h 同服务回放返回 `infeasible`。真实全年与代表期的零燃料自然最小弃电分别为 `565,916.122` 与 `338,704.669 MWh`，聚合低估 `227,211.453 MWh`。

baseline 已违反原合同的可行性一致性门，因此 R1 整体登记为失败；不以尚未完成的其他架构或状态覆盖该判定。完整证据、陈旧批次排除与周级诊断见 `e0_d38r1_baseline_temporal_aggregation_failure.md`。
