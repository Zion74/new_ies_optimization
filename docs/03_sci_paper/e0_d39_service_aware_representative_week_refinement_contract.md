# E0-D-39 服务感知代表周一次性增量修订合同

状态：**Gate A 通过；Gate B 定量保真失败；Gate C/D 禁止启动，D39 已终止**

适用范围：D38-R1 baseline 时间聚合失败后的单次、可审计代表期修订

日期：2026-07-15

## 1. 修订原因与不可覆盖边界

D36 仅用热负荷、风电 CF、光伏 CF 和气温的 672 维曲线距离构造六个代表周。D38-R1 正式 baseline 链证明：D36/D37 代表期无储能在真实全年 10% 弃电帽与同一 PCC 目标下可行，而真实 8784 h 回放不可行；零燃料自然最小弃电被低估 `227,211.4531313089 MWh`。

D39 只执行 D38 原合同第 8 节允许的“失败后识别误差最大实际周、另开结果前合同增加 1–2 周并重建全部权重”。D36 数据包、原 D38 高热失败、R1 合同和 baseline 失败记录永久保留，不能被 D39 覆盖或改写为通过。

## 2. 锁定输入与失败诊断

- 热负荷 SHA-256：`a89d3654600eac53768529ad9ef6d304b7d756783359fc1f1db95fd2bd4c709e`；
- 风光与气温 SHA-256：`515892a944dacf75c4bae3f41f008b01924f30dbd9b004d132afbdb7c0e25b6f`；
- D36 assignments SHA-256：`31c7daae3faa5ffa91f3e5b31ad75fc666cf9f3952bac399352ec832607488a3`；
- D36 periods SHA-256：`02b168d6b4169101c1d601a548c7a475d8aea8a8a280de5f52fcaaf6ec09aaa9`；
- D38-R1 baseline 周级诊断 SHA-256：`3ea98ef46b72705617a3dc436c57b158f7819aa8e358c8d85448e66f5bc46329`；
- D38-R1 baseline 服务合同 SHA-256：`93f3d7b5c50312d08ea3dd78b1af70661facf880fcc882b1bb1ac32a783977b3`。

失败诊断必须来自 baseline、无储能、零燃料最小弃电模型；不得使用 BESS/TES/Hybrid 的容量、成本、后悔值或赢家排序选周。

## 3. 一次性选周规则

原 D36 六个代表周 `4/5/8/29/39/48` 全部保留，不重新运行 PAM，也不删除任何原强制极端角色。

对 `baseline_weekly_failure_diagnostic.json` 的 52 个实际周，按 `curtailment_underrepresentation_mwh` 降序排列；数值并列时取较小周序号。选择尚不在 D36 集合中的前两个周，一次性加入代表集。冻结结果为：

1. 第 49 周，2024-12-02 至 12-08，分配到原代表周 8 时低估 `20,547.320280718035 MWh`；
2. 第 16 周，2024-04-15 至 04-21，分配到原代表周 39 时低估 `20,063.70159011765 MWh`。

D39 最终代表周固定为按周序排列的 `4/5/8/16/29/39/48/49`。第 49/16 周恰好来自累计低估最大的两个原簇 8/39，但“簇多样性”不是另一个可调规则。D39 不再增加、替换或搜索其他周；若失败，必须登记负结果并另立新合同。

## 4. 特征、重分配与年尾段

全部 52 周使用 D36 完全相同的四通道、通道总体均值/总体标准差、672 维拼接顺序和标准化平方欧氏距离。D39 不重新选择 medoid，只将每个实际周分配给八个冻结代表周中距离最近者；距离并列时取较小代表周序号。

每个代表周必须分配给自身，八个权重均至少为 1，权重之和为 52。全部权重必须重建，不允许只给新增周手工赋权或保留 D36 旧权重。

年末 72 h 块保持 D36 原始源时标与行值：2024-12-29 00:00–23:00 为不计分 warm-up，12 月 30–31 日 48 h 为权重 1 的计分段。D39 数据包应包含：

- `8×168 + 24 + 48 = 1416` 个模型时段；
- `8×168 + 48 = 1392` 个计分源行；
- 加权计分小时严格为 `8784 h`；
- 八个代表周继续按 D37 块内独立循环，年尾块边界不变。

## 5. 文件隔离与 provenance

D39 规范数据目录固定为 `风光火+熔盐储热/数据采集/e0d39_service_aware_representative_weeks/`，服务器结果目录固定为 `/root/e0-b-20260711-019f4f64/results/e0d39_service_aware_refinement/`。

规范数据文件固定为：

- `e0d39_week_assignments.csv`；
- `e0d39_representative_periods.csv`；
- `manifest.json`；
- 非规范执行 sidecar `execution.json`。

manifest 必须记录本合同、D36 两个规范哈希、D38 周级诊断哈希、精确代表集、重分配规则、代码哈希和全部输出哈希。D39 不覆盖任何 `e0d36_*` 或 `e0d38_*` 文件。

D39 的正式服务与案例文件名必须包含 `d39` 或位于 D39 独立结果目录。由于 D38 服务 provenance 含 D36 periods 哈希，D39 必须用 D39 periods 重新生成同算法 baseline 服务，不得把 D38 服务 JSON 直接冒充为 D39 服务。物理状态、求解算法和服务参数不得改变。

## 6. Gate A：数据构造验收

必须同时满足：

1. 输入和诊断哈希与第 2 节完全一致；
2. 代表集严格为 `4/5/8/16/29/39/48/49`，52 周恰好分配一次，权重之和为 52；
3. 特征尺度、距离与 tie-break 和 D36 一致；
4. 1416 个模型时段、1392 个计分源行和 8784 加权小时通过；
5. 年尾 72 h 的源行、数值、计分标志和权重与 D36 逐字段一致；
6. Windows 与 OpenBayes 三个规范文件逐字节一致；
7. 构造阶段不调用规划求解器，不生成储能容量或技术排序。

任一项失败即停止，不进入 Gate B。

## 7. Gate B：baseline 零燃料保真门

只运行 baseline 无储能零燃料最小弃电，分别使用：

- 真实 8784 h 单循环块；
- D39 八周加年尾段的九块独立循环时域。

求解器仅 HiGHS，随机种子 0，正式 gap `0.1%`，第一阶段沿用已验证的连续等价零燃料段编码，不改变 CHP 出力、供热、启停、爬坡、PCC 或弃电可行域投影。

Gate B 必须同时满足：

1. 两个结果均有有限 incumbent/dual 且实际 gap 不超过 `0.1%`；
2. 真实全年自然最小弃电高于 10% 弃电帽至少 `1e-3 MWh`；
3. D39 代表期自然最小弃电也高于同一绝对 10% 帽至少 `1e-3 MWh`，即两者对 no-storage 服务可行性的分类一致；
4. 两者以真实全年可再生可用量为共同分母的自然最小弃电率绝对误差不超过 1 个百分点。

若只恢复可行性分类但误差超过 1 个百分点，D39 仍登记为失败，不得放宽阈值。

## 8. Gate C：正式 baseline 同服务一致性

Gate B 通过后，按 D38/R1 原算法重新生成 D39 baseline 两阶段服务：先最小弃电，再在自然最小弃电 `+1e-3 MWh/a` 内最小燃料并取得年度 PCC 目标。10% 弃电帽、PCC 700 MW、价格、gap 和全部物理参数不变。

随后执行 baseline no-storage 代表期规划与真实 8784 h 回放。因为 Gate B 已表明自然最小弃电高于 10% 帽，二者都应被规范登记为 `infeasible`；不得用罚值、伪成本或遗漏结果表示。只有代表期与全年可行性状态一致，且服务/provenance 审计通过，Gate C 才通过。

## 9. Gate D：恢复 R1 三状态预验证

只有 Gate A–C 全部通过，才允许用 D39 时域重新执行 D38-R1 的 baseline、`high_heat_tight_pcc_r1`、`long_duration_24h` 三状态与 No storage、BESS、TES、Hybrid 四架构。

除代表期 periods/assignments 外，原 D38/R1 的状态、10% 弃电帽、PCC 目标生成、完整容量快照、0.1% gap、`1e-3 MWh` 服务残差、3% 成本、1 个百分点弃电、3% 燃煤、2% 后悔值、10% 容量和 5% 无差异集合规则全部保持不变。任一状态或架构失败即停止，不启动 E2/E3/E4。

## 10. 主张边界与 agentic 定位

D39 最多允许报告：

- 原纯输入特征代表周为何在运行可行性上失败；
- 一次性、审计可追溯的服务感知增量修订是否恢复了分类和定量保真；
- D39 通过或失败及相应误差。

D39 不产生杨凌正式 TAC、项目技术赢家或正式 E5 全部结果。它可作为第 5 章 Agentic 决策支持的一个“失败检测—规则触发—证据冻结—再验证”示例，但 agent 只负责合同化编排与审计，不能修改优化器物理、阈值或根据赢家继续试周。

## 11. Gate A 执行后登记（不修改前述规则）

结果前合同提交 `7ba61a4` 后才实现并运行 D39 构造器。Windows 与 OpenBayes 独立构造均得到：

- 代表周 `4/5/8/16/29/39/48/49`，权重 `1/2/9/2/13/19/4/2`；
- 1416 个模型时段、1392 个计分源行、8784 加权小时；
- 年尾 72 h 与 D36 除模型期编号/块序号外的合同字段逐项一致；
- assignments、periods、manifest SHA-256 分别为 `7949d6f58d86787cf9ea8129dae3adc85ec20ffba8a157ad7e121395f2f5052e`、`fb7aa1e9d8815a2a22eee68b61af12b44c4485ba3ca464d21652480d9b75c2ac`、`dabb565087e9adb2e597d00ea7c12fcb30bf9e522517a7f8e6ed7ee73d9a16a9`，双端逐字节一致；
- 描述性误差为热量 `+4.8512%`、风电 `-5.8068%`、光伏 `+2.6222%`、均温 `+0.9442 °C`；
- D39 定向回归双端各 `10 passed`，完整回归为 Windows `452 passed in 67.31s`、OpenBayes `452 passed in 33.51s`。

Gate A 到此通过；上述误差不授权继续选周。下一步只能执行 Gate B baseline 零燃料保真门。

Gate B 首次命令在求解器启动前被 D37 的 D36-only period SHA 锁拒绝，没有生成任何数值结果。接入修订保留 D36 默认锁，仅允许文件名、SHA、块顺序、权重和 1416 时段全部匹配的 D39 period 数据；D39 assignments 另受文件名与 SHA 双锁。修订后 Windows/OpenBayes 完整回归均为 `454 passed`。该修订只补足预注册 D39 数据的严格读取能力，不改变 Gate B 物理、服务、目标或阈值。

## 12. Gate B 执行后登记

正式结果中，真实全年与 D39 八周自然最小弃电分别为 `565,916.1220067485` 与 `390,148.306132333 MWh`，均高于 `339,569.90645758656 MWh` 的 10% 帽，分类一致；但共同真实可再生可用量分母下的弃电率误差为 `5.17618942467652` 个百分点，超过冻结的 `1.0` 个百分点阈值。故 `passed=false`，D39 在 Gate B 失败。

Gate B JSON SHA-256 为 `47f33db2d3a00bbe5f70cd342198fd5daa1538663c49b8ec7d39641fd27b645b`。按前述不可协商规则，不执行 Gate C/D，不继续增加 D39 周数，不放宽阈值。完整解释见 `e0_d39_gate_b_quantitative_fidelity_failure.md`。
